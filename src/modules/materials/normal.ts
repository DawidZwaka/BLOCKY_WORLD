/*import MainScene from '../scene';
import * as BABYLON from 'babylonjs';

const material = () => {
    const sc = new MainScene();

    const normalMaterial = new BABYLON.StandardMaterial("normal", sc.getScene());
    normalMaterial.diffuseColor = new BABYLON.Color3(1, 0, 1);
    normalMaterial.wireframe = true;

    return normalMaterial;
}

export default material;*/